# دليل استخدام لوحة التحكم - Trade for Egypt

## 🎉 تم بناء لوحة التحكم بنجاح!

تم إنشاء لوحة تحكم متقدمة وشاملة لإدارة ومعاينة جميع مواقع الشبكة.

---

## 📂 الملفات المنشأة

### API Routes
1. `/pages/api/admin/stats.js` - جلب الإحصائيات العامة
2. `/pages/api/admin/sites.js` - جلب جميع المواقع مع تقدم التوليد
3. `/pages/api/admin/pages.js` - جلب صفحات ماركة معينة

### صفحات لوحة التحكم
1. `/pages/admin/index.js` - Dashboard الرئيسي
2. `/pages/admin/sites.js` - عرض جميع المواقع
3. `/pages/admin/sites/[brand].js` - عرض صفحات ماركة معينة
4. `/pages/admin/preview/[brand]/[product]/[keyword].js` - معاينة صفحة

---

## 🚀 كيفية التشغيل

### 1. تشغيل المشروع محليًا

```bash
cd /home/ubuntu/tradeforegypt
npm run dev
```

### 2. فتح لوحة التحكم

افتح المتصفح واذهب إلى:
```
http://localhost:3000/admin
```

---

## 🎯 المميزات الرئيسية

### 1. Dashboard الرئيسي (`/admin`)

**الإحصائيات المعروضة:**
- ✅ عدد العائلات: 26
- ✅ عدد الماركات: 352
- ✅ عدد المنتجات: 113
- ✅ عدد الصفحات المولدة: 0 (حاليًا)
- ✅ عدد الصفحات المتوقعة: ~238,656

**Progress Bar:**
- شريط تقدم حي يوضح نسبة الإنجاز
- يتم حسابه تلقائيًا: (الصفحات المولدة / الصفحات المتوقعة) × 100

**آخر الماركات المضافة:**
- عرض آخر 5 ماركات تم إضافتها
- روابط سريعة لعرض صفحات كل ماركة

**توزيع الماركات حسب العائلات:**
- رسم بياني يوضح عدد الماركات في كل عائلة
- يساعد في فهم توزيع المحتوى

**إجراءات سريعة:**
- عرض جميع المواقع
- توليد الصفحات (قريبًا)
- تحديث البيانات

---

### 2. صفحة المواقع (`/admin/sites`)

**عرض جميع المواقع (352 موقع):**
- اسم الماركة
- السبدومين (مثال: zanussi.tradeforegypt.com)
- عدد العائلات المرتبطة
- Progress Bar لكل موقع
- عدد الصفحات المولدة / المتوقعة
- حالة الموقع (مكتمل / قيد التنفيذ / قيد الانتظار)

**الفلاتر:**
- بحث بالاسم أو slug
- تصفية حسب الحالة (الكل / مكتمل / قيد التنفيذ / قيد الانتظار)

**إحصائيات سريعة:**
- إجمالي المواقع
- عدد المواقع المكتملة
- عدد المواقع قيد التنفيذ
- عدد المواقع قيد الانتظار

**الإجراءات:**
- زر "عرض الصفحات" → يأخذك لصفحة تفاصيل الموقع
- زر "فتح الموقع" → يفتح السبدومين في نافذة جديدة

---

### 3. صفحة تفاصيل الموقع (`/admin/sites/[brand]`)

**مثال:** `/admin/sites/zanussi`

**Progress Bar للموقع:**
- نسبة الإنجاز الكاملة
- عدد الصفحات المولدة
- عدد الصفحات المتوقعة
- عدد الصفحات قيد الانتظار

**جدول جميع الصفحات:**
- المنتج (عربي وإنجليزي)
- الكلمة البحثية (عربي)
- المسار الكامل
- الحالة (مولدة / قيد الانتظار)
- إجراءات (معاينة / فتح)

**الفلاتر:**
- بحث بالمنتج
- تصفية حسب الكلمة البحثية (توكيل / صيانة / ضمان / خط ساخن / أرقام / خدمة عملاء)
- تصفية حسب الحالة (الكل / مولدة / قيد الانتظار)

**مثال على الصفحات:**
```
/zanussi/washing-machine/agency
/zanussi/washing-machine/maintenance
/zanussi/fridge/guarantee
/zanussi/cooker/hotline
... إلخ
```

---

### 4. صفحة المعاينة (`/admin/preview/[brand]/[product]/[keyword]`)

**مثال:** `/admin/preview/zanussi/washing-machine/maintenance`

**4 تبويبات رئيسية:**

#### أ) معاينة الصفحة
- عرض مباشر للصفحة في iframe
- خيارات عرض: Desktop / Mobile
- زر "فتح في نافذة جديدة"

#### ب) Meta Tags
- **Title Tag:** العنوان الكامل للصفحة
- **Meta Description:** الوصف (150-160 حرف)
- **Canonical URL:** الرابط الكنسي
- **Keywords:** الكلمات المفتاحية

**روابط اختبار SEO:**
- Google Rich Results Test
- Facebook Open Graph Debugger
- Twitter Card Validator

#### ج) Schema Markup
عرض جميع أنواع Schema (JSON-LD):
1. **Organization Schema**
2. **Product Schema**
3. **LocalBusiness Schema**
4. **Breadcrumb Schema**
5. **WebPage Schema**

**رابط اختبار:**
- Google Rich Results Test

#### د) معلومات الصفحة
- الماركة
- المنتج
- الكلمة البحثية
- السبدومين
- المسار الكامل
- المسار النسبي

---

## 📊 البيانات الحالية

### الإحصائيات الفعلية من API:

```json
{
  "stats": {
    "totalFamilies": 26,
    "totalBrands": 352,
    "totalProducts": 113,
    "totalRelations": 0,
    "generatedPages": 0,
    "expectedPages": 238656,
    "progressPercentage": 0
  }
}
```

**ملاحظة:** عدد الصفحات المتوقعة = 352 ماركة × 113 منتج × 6 كلمات = 238,656 صفحة!

---

## 🎨 التصميم

### الألوان:
- **Primary:** أزرق (#3B82F6)
- **Success:** أخضر (#10B981)
- **Warning:** برتقالي (#F59E0B)
- **Danger:** أحمر (#EF4444)

### المكونات:
- Cards للإحصائيات
- Tables للبيانات
- Progress Bars متحركة
- Tabs للتنقل
- Filters للبحث والتصفية
- Responsive Design (يعمل على جميع الأجهزة)

---

## 🔄 سير العمل

### 1. الدخول إلى لوحة التحكم
```
http://localhost:3000/admin
```

### 2. عرض الإحصائيات
- شاهد الإحصائيات العامة
- تابع Progress Bar
- اطلع على آخر الماركات المضافة

### 3. عرض المواقع
- اضغط "عرض جميع المواقع"
- ابحث عن ماركة معينة
- صفّ حسب الحالة

### 4. عرض صفحات موقع معين
- اضغط "عرض الصفحات" على أي موقع
- شاهد جميع الصفحات المتوقعة
- استخدم الفلاتر للبحث

### 5. معاينة صفحة
- اضغط "معاينة" على أي صفحة
- شاهد الصفحة مباشرة
- تحقق من Meta Tags و Schema
- اختبر في Google Rich Results

---

## ⚠️ ملاحظات مهمة

### 1. الصفحات الديناميكية
حاليًا، الصفحات الديناميكية (`/[brand]/[product]/[keyword].js`) **لم يتم إنشاؤها بعد**.

لذلك، عند الضغط على "معاينة"، قد تحصل على خطأ 404.

**الحل:**
- يجب إنشاء الصفحة الديناميكية أولاً (المرحلة التالية)
- بعد ذلك ستعمل جميع روابط المعاينة

### 2. general_stats View
تأكد من تنفيذ SQL لإنشاء `general_stats` view:

```sql
CREATE VIEW general_stats AS
SELECT
  (SELECT COUNT(*) FROM families) AS total_families,
  (SELECT COUNT(*) FROM brands) AS total_brands,
  (SELECT COUNT(*) FROM products) AS total_products,
  (SELECT COUNT(*) FROM brand_families) AS total_relations,
  (SELECT COUNT(*) FROM page_content) AS total_custom_pages;
```

### 3. brand_families
لاحظ أن `total_relations` = 0 حاليًا، مما يعني أن جدول `brand_families` فارغ.

**يجب ملء هذا الجدول لربط الماركات بالعائلات.**

---

## 🚀 الخطوات التالية

### المرحلة التالية: إنشاء الصفحات الديناميكية

1. **إنشاء `/pages/[brand]/[product]/[keyword].js`**
   - تطبيق `getStaticPaths`
   - تطبيق `getStaticProps`
   - توليد المحتوى الديناميكي

2. **إنشاء المكونات المساعدة:**
   - `components/SEOHead.js`
   - `components/SchemaMarkup.js`
   - `lib/seo-generator.js`
   - `lib/schema-generator.js`

3. **اختبار المعاينة:**
   - بعد إنشاء الصفحات الديناميكية
   - جرب المعاينة من لوحة التحكم
   - تحقق من Meta Tags و Schema

---

## 📸 لقطات شاشة (وصف)

### Dashboard الرئيسي:
- 4 بطاقات إحصائيات في الأعلى
- Progress Bar كبير في المنتصف
- قسمين جانبيين: آخر الماركات + توزيع العائلات
- 3 أزرار إجراءات سريعة في الأسفل

### صفحة المواقع:
- 4 بطاقات إحصائيات صغيرة
- شريط فلاتر (بحث + حالة)
- Grid من البطاقات (3 أعمدة)
- كل بطاقة تحتوي على: اسم، سبدومين، Progress Bar، أزرار

### صفحة تفاصيل الموقع:
- Header مع شعار الماركة
- Progress Bar كبير مع 3 إحصائيات
- شريط فلاتر (بحث + كلمة + حالة)
- جدول كبير بجميع الصفحات

### صفحة المعاينة:
- Header مع معلومات الصفحة
- 4 تبويبات (معاينة / Meta / Schema / معلومات)
- iframe للمعاينة المباشرة
- روابط اختبار SEO

---

## ✅ الخلاصة

تم بناء لوحة تحكم **احترافية وشاملة** تحتوي على:

1. ✅ Dashboard إحصائي مع Progress Bar
2. ✅ عرض جميع المواقع (352 موقع)
3. ✅ عرض صفحات كل موقع (حتى 684 صفحة لكل موقع)
4. ✅ نظام معاينة متقدم مع 4 تبويبات
5. ✅ فلاتر وبحث متقدم
6. ✅ تصميم responsive وجذاب
7. ✅ روابط اختبار SEO

**المشروع جاهز للانتقال للمرحلة التالية: إنشاء الصفحات الديناميكية!** 🚀
