# نظام توليد الصفحات - Trade for Egypt

## نظرة عامة

نظام توليد الصفحات يقوم بإنشاء صفحات ديناميكية لجميع تركيبات:
- **الماركات** (263 مصنفة)
- **المنتجات** (129 منتج)
- **الكلمات المفتاحية** (6 كلمات)

**الناتج المتوقع:** ~200,000 صفحة

---

## الكلمات المفتاحية الستة

1. **توكيل** - وكيل معتمد
2. **صيانة** - مركز صيانة
3. **ضمان** - معلومات الضمان
4. **خدمة عملاء** - خدمة العملاء
5. **خط ساخن** - رقم الخط الساخن
6. **أرقام** - أرقام التواصل

---

## بنية URL

```
https://{brand-slug}.tradeforegypt.com/{product-slug}/{keyword-slug}
```

**أمثلة:**
- `https://samsung.tradeforegypt.com/ghsalh/twkyl` (توكيل غسالة سامسونج)
- `https://lg.tradeforegypt.com/thlajh/syanh` (صيانة ثلاجة LG)
- `https://bosch.tradeforegypt.com/mkyfh/dman` (ضمان مكيفة بوش)

---

## بنية قاعدة البيانات

### جدول `pages`

```sql
CREATE TABLE pages (
  id BIGSERIAL PRIMARY KEY,
  brand_id BIGINT REFERENCES brands(id),
  product_id BIGINT REFERENCES products(id),
  keyword VARCHAR(50), -- 'توكيل', 'صيانة', 'ضمان', إلخ
  slug VARCHAR(255) UNIQUE NOT NULL,
  title TEXT NOT NULL,
  meta_description TEXT,
  h1 TEXT,
  content TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

---

## استراتيجية التوليد

### المرحلة 1: التوليد التجريبي
- اختيار عائلة واحدة (الأجهزة المنزلية الكبيرة)
- توليد جميع صفحاتها (~1,200 صفحة)
- اختبار الجودة والأداء

### المرحلة 2: التعميم
- تطبيق على باقي 26 عائلة
- توليد ~200,000 صفحة
- النشر التدريجي

---

## قوالب المحتوى

### 1. توكيل (Authorized Dealer)

```
# توكيل {منتج} {ماركة}

هل تبحث عن **توكيل {ماركة} المعتمد** لـ {منتج}؟ نحن الوكيل الرسمي لـ {ماركة} في مصر.

## لماذا التوكيل المعتمد؟
- ضمان أصلي من الشركة
- قطع غيار أصلية
- فنيين معتمدين
- خدمة ما بعد البيع

## معلومات التواصل
- الخط الساخن: [رقم]
- العنوان: [عنوان]
- ساعات العمل: [أوقات]
```

### 2. صيانة (Maintenance)

```
# صيانة {منتج} {ماركة}

مركز صيانة {ماركة} المعتمد لـ {منتج}. خدمة صيانة احترافية بقطع غيار أصلية.

## خدمات الصيانة
- صيانة دورية
- إصلاح الأعطال
- قطع غيار أصلية
- ضمان على الصيانة

## اتصل بنا
- رقم الصيانة: [رقم]
- خدمة منزلية متاحة
```

### 3. ضمان (Warranty)

```
# ضمان {منتج} {ماركة}

معلومات شاملة عن ضمان {منتج} {ماركة} في مصر.

## شروط الضمان
- مدة الضمان: [مدة]
- ما يغطيه الضمان
- ما لا يغطيه الضمان
- كيفية تفعيل الضمان

## للاستفسار
- خدمة العملاء: [رقم]
```

### 4. خدمة عملاء (Customer Service)

```
# خدمة عملاء {ماركة} - {منتج}

تواصل مع خدمة عملاء {ماركة} للحصول على الدعم الفني والاستفسارات.

## كيف نساعدك؟
- استفسارات المنتجات
- الدعم الفني
- الشكاوى والاقتراحات
- متابعة الطلبات

## التواصل
- الهاتف: [رقم]
- البريد: [email]
- الدردشة المباشرة
```

### 5. خط ساخن (Hotline)

```
# الخط الساخن {ماركة} - {منتج}

رقم الخط الساخن لـ {ماركة} متاح 24/7 لخدمتك.

## الخط الساخن
📞 **[رقم الخط الساخن]**

متاح على مدار الساعة لتلقي:
- البلاغات العاجلة
- الأعطال الطارئة
- الاستفسارات السريعة
```

### 6. أرقام (Contact Numbers)

```
# أرقام {ماركة} - {منتج}

جميع أرقام التواصل مع {ماركة} في مكان واحد.

## أرقام الاتصال
- المبيعات: [رقم]
- الصيانة: [رقم]
- خدمة العملاء: [رقم]
- الخط الساخن: [رقم]
- الشكاوى: [رقم]
```

---

## SEO Optimization

### Title Pattern
```
{keyword} {product} {brand} | {alternative_name} - Trade for Egypt
```

**مثال:**
```
توكيل غسالة سامسونج | Samsung - Trade for Egypt
```

### Meta Description Pattern
```
{keyword} {product} {brand} المعتمد في مصر. {alternative_names}. خدمة احترافية، قطع غيار أصلية، ضمان معتمد. اتصل الآن!
```

### H1 Pattern
```
{keyword} {product} {brand} المعتمد في مصر
```

---

## التنفيذ

### سكريبت التوليد

```javascript
// scripts/generate-pages.js

const { createClient } = require('@supabase/supabase-js');

const keywords = [
  { slug: 'twkyl', name: 'توكيل', template: 'dealer' },
  { slug: 'syanh', name: 'صيانة', template: 'maintenance' },
  { slug: 'dman', name: 'ضمان', template: 'warranty' },
  { slug: 'khdmh-amla', name: 'خدمة عملاء', template: 'customer-service' },
  { slug: 'kht-sakhn', name: 'خط ساخن', template: 'hotline' },
  { slug: 'arqam', name: 'أرقام', template: 'numbers' }
];

async function generatePages(familyId) {
  // 1. جلب الماركات في العائلة
  const brands = await getBrandsByFamily(familyId);
  
  // 2. جلب المنتجات في العائلة
  const products = await getProductsByFamily(familyId);
  
  // 3. توليد الصفحات
  for (const brand of brands) {
    for (const product of products) {
      for (const keyword of keywords) {
        await generatePage(brand, product, keyword);
      }
    }
  }
}
```

---

## الخطوات التالية

1. ✅ إنشاء قوالب المحتوى
2. ⏳ بناء سكريبت التوليد
3. ⏳ اختبار على عائلة واحدة
4. ⏳ تحسين وضبط
5. ⏳ التعميم على جميع العائلات
6. ⏳ النشر على Vercel
